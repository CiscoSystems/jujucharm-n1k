[
    {rabbit, [    
    	{ssl_listeners, [{{ ssl_port }}]},
    	{ssl_options, [
        	{certfile, "{{ ssl_cert_file }}"},
        	{keyfile, "{{ ssl_key_file }}"}
    	]},
    	{tcp_listeners, [5672]}
    ]}
].    